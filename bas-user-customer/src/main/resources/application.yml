server:
  port: 8083

spring:
  application:
    name: user-customer

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
     defaultZone: http://${eureka.instance.hostname}:8081/eureka/



